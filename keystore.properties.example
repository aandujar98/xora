# Optional production / Play signing. Copy to keystore.properties (gitignored).
# When absent, release APKs fall back to the shared repo debug.keystore.
storeFile=path/to/your-release.keystore
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
