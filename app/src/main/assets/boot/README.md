# Boot intro

Cold-start clip played once when XOrA opens after a full close.

| File | Role |
|------|------|
| `bootup.mp4` | 1080p 60 fps H.264 + AAC, ends on white |

Encoded from the `xora-boot` GitHub release (`_.BOOTUP.mp4`). After the last white frame the shell fades to the XMB.
